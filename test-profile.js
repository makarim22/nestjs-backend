const http = require('http'); async function main() { try { const res = await fetch('http://localhost:3000/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'testapi@example.com', password: 'password' }) }); const data = await res.json(); const token = data.access_token; const pRes = await fetch('http://localhost:3000/users/profile', { headers: { 'Authorization': 'Bearer ' + token } }); console.log('profile status:', pRes.status); console.log('profile:', await pRes.text()); } catch(e) { console.error(e); } } main();
