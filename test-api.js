const http = require('http'); async function main() { try { const res1 = await fetch('http://localhost:3000/auth/signup', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'testapi@example.com', password: 'password', name: 'Test API' }) }); let data = await res1.json(); if (!data.access_token) { const res2 = await fetch('http://localhost:3000/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'testapi@example.com', password: 'password' }) }); data = await res2.json(); } const token = data.access_token; console.log('token:', token); const saveRes = await fetch('http://localhost:3000/users/save/movie/2b94e5cf-5765-42d6-8b9e-083992aaae41', { method: 'POST', headers: { 'Authorization': 'Bearer ' + token } }); console.log('status:', saveRes.status); console.log('response:', await saveRes.text()); } catch(e) { console.error(e); } } main();
